{#
/**
 * @file
 * Theme override to display a node of bundle event in the teaser view mode.
 */
#}
{{ pattern('list_item', {
  'variant': 'date',
  'url': url,
  'title': title,
  'meta': meta,
  'detail': content.oe_teaser,
  'date': date,
  'icons_text': icons_text
}) }}
